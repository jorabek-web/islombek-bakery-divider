import { ParkashNotification } from "@/page-ui";

const Notification = () => {
  return (
    <div className="pt-[15px]">
      <ParkashNotification />
    </div>
  );
};

export default Notification;
